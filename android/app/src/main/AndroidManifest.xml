<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

  <!-- WebView 안의 navigator.geolocation 용. 매니페스트에 선언이 없으면
       안드로이드가 런타임 권한 팝업 자체를 띄우지 않고 즉시 거부한다. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

  <!-- 위치 권한을 선언하면 안드로이드가 위치 하드웨어를 "필수"로 간주해
       GPS 없는 기기가 Play 배포 대상에서 제외된다. 위치는 부가 기능이므로
       required="false" 로 명시해 기존 지원 기기 범위를 유지한다. -->
  <uses-feature android:name="android.hardware.location" android:required="false" />
  <uses-feature android:name="android.hardware.location.gps" android:required="false" />
  <uses-feature android:name="android.hardware.location.network" android:required="false" />

  <!--
    enableOnBackInvokedCallback: targetSdk 36(Android 16)부터 이 값의 기본이 true로 바뀌어
    predictive back이 켜지고, 그러면 레거시 back 디스패치(onBackPressed/KEYCODE_BACK)가 더 이상
    호출되지 않는다. React Native의 BackHandler가 그 경로에 의존하므로 hardwareBackPress가
    아예 뜨지 않아, 웹뷰로 backAction 메시지를 보내는 코드가 실행되지 않는다(뒤로가기 먹통).
    어바웃 앱은 targetSdk 35라 기본이 false여서 정상 동작한다. 명시적으로 꺼서 동작을 맞춘다.
  -->
  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:allowBackup="false"
    android:enableOnBackInvokedCallback="false"
    android:theme="@style/AppTheme">

    <!-- ... 생략(기존 그대로) ... -->

    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:exported="true">

      <!-- 기본 런처 인텐트 -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!-- ✅ (기존) 앱 스킴 -->
      <intent-filter android:label="DeepLink">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
       <data android:scheme="kagongmap" />
      </intent-filter>

<!-- study-about.club -->
<!-- <intent-filter android:autoVerify="true">
  <action android:name="android.intent.action.VIEW" />
  <category android:name="android.intent.category.DEFAULT" />
  <category android:name="android.intent.category.BROWSABLE" />
  <data android:scheme="https" android:host="study-about.club" />
</intent-filter>
<intent-filter android:autoVerify="true">
  <action android:name="android.intent.action.VIEW" />
  <category android:name="android.intent.category.DEFAULT" />
  <category android:name="android.intent.category.BROWSABLE" />
  <data android:scheme="https" android:host="www.study-about.club" />
</intent-filter> -->

<intent-filter android:label="KakaoLink">
  <action android:name="android.intent.action.VIEW" />
  <category android:name="android.intent.category.DEFAULT" />
  <category android:name="android.intent.category.BROWSABLE" />
  <data
    android:scheme="kakao0ab4a527fe2b99e3c18e904244a97e20"
    android:host="kakaolink" />
</intent-filter>


      <!-- ✅ (추가) https://about20s.club/* 전부 앱이 가로채기 -->
      <!-- <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
          android:scheme="https"
          android:host="about20s.club"
          android:pathPrefix="/" />
      </intent-filter> -->

      <!-- ✅ (추가) https://www.about20s.club/* 도 쓰면 이것도 -->
      <!-- <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
          android:scheme="https"
          android:host="www.about20s.club"
          android:pathPrefix="/" />
      </intent-filter> -->

    </activity>
  </application>
</manifest>
